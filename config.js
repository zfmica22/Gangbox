/* ============================================================
   GANGBOX configuration
   ------------------------------------------------------------
   Paste your Supabase project URL and anon (public) key below,
   then save and redeploy. Both are safe to expose publicly —
   the anon key is designed for browser apps.

   Leave them blank to run in LOCAL-ONLY mode (data stays on
   this one device, nothing syncs).

   Where to find these: Supabase dashboard → your project →
   Project Settings → API → "Project URL" and "anon public" key.
   ============================================================ */
window.GANGBOX_CONFIG = {
  url: "https://byeocoqaxzvtaxmehbny.supabase.co",
  anonKey: "sb_publishable_iR9kbjp6-fpuSheoAzYATw_S0Gzl9IE"
};
